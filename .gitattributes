# GitHub Pages must serve the media files directly, so keep images as normal Git blobs.
# Keep only the oversized BIM model in LFS because it exceeds GitHub's 100 MB limit.
assets/projects/bim/*.obj filter=lfs diff=lfs merge=lfs -text
